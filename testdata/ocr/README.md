# OCR test images

Real-world screenshots for checking OCR and hit testing: manga pages, social feeds, articles, vertical text.

Use them through the OCR debug screen in the app (push to the device with `adb push <file> /sdcard/Pictures/`).
